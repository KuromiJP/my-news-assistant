# copy to .env (do not commit)
PORT=8080
BASIC_AUTH_USER=hotdog
BASIC_AUTH_PASS=change-me
# Optional: serve generated summaries from outside the repo
# PUBLIC_DIR=/home/admin/.openclaw/data/my-news-assistant/public
